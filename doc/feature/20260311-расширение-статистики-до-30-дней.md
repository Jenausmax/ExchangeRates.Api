- [x] Реализовано

# План реализации: Расширение статистики до 30 дней для Telegram бота

## Контекст

Пользователям требуется более гибкий инструмент для анализа динамики курсов валют. Текущая реализация ограничена фиксированными периодами (1 день и 7 дней). Новая фича добавляет возможность выбора произвольного периода от 3 до 30 дней с адаптивным форматированием сообщений для обеспечения читаемости даже при больших объемах данных.

## Обзор решения

Фича добавляет кнопку "Статистика" в ReplyKeyboard бота и текстовую команду `/statistics`. При активации пользователь получает inline клавиатуру с выбором периода (3, 7, 14, 21, 30 дней). Формат сообщения адаптируется к выбранному периоду:
- **1-7 дней**: детальный формат с ежедневными данными
- **8-14 дней**: смешанный формат (последние 3 дня + агрегированная статистика)
- **15-30 дней**: компактный формат (только агрегированная статистика с эмодзи)

## UX дизайн

### Поток взаимодействия

```
Пользователь → Нажимает "Статистика" или вводит /statistics
              → Бот показывает inline клавиатуру с вариантами (3, 7, 14, 21, 30 дней)
              → Пользователь выбирает количество дней
              → Бот формирует и отправляет отчет в соответствии с периодом
```

### Inline клавиатура выбора периода

```
📊 Выберите период для статистики:

[3 дня] [7 дней] [14 дней]
[21 день] [30 дней]
```

### Примеры сообщений

**Короткий период (3 дня) - детальный формат:**
```
USD/RUB -- Доллар США
━━━━━━━━━━━━━━━━━
11 мар  92.50 → -0.12 (-0.13%)
10 мар  92.62 → +0.05 (+0.05%)
09 мар  92.57

 Макс: 92.62 (10 мар)
 Мин:  92.50 (11 мар)
 Изм:  -0.12 (-0.13%) за 3 дн.
```

**Средний период (14 дней) - смешанный формат:**
```
📊 USD/RUB -- Доллар США (14 дней)

📊 Последние дни:
 11 мар  92.50 → -0.12 (-0.13%)
 10 мар  92.62 → +0.05 (+0.05%)
 09 мар  92.57

📈 Статистика за период:
 ↑ Макс: 93.15 (25 фев)
 ↓ Мин:  92.50 (11 мар)
 ↔ Изм:  -0.60 (-0.64%) за 14 дн.
```

**Длинный период (30 дней) - компактный формат:**
```
📊 USD/RUB -- Доллар США (30 дней)

💰 Текущий: 92.50 руб.

📈 Динамика:
 ↑ Макс: 93.15 руб. (25 фев)
 ↓ Мин:  92.50 руб. (11 мар)
 ↔ Изм:  -0.60 (-0.64%)

📉 Тренд за период:
 Начало периода: 93.10 руб.
 Конец периода:  92.50 руб.
 Средний курс:   92.80 руб.
```

## Эмодзи для визуализации

- 📊 - заголовок статистики
- 💰 - текущий курс
- 📈 - максимальное значение и тренд вверх
- 📉 - минимальное значение и тренд вниз
- ↔ - стабильность (минимальные изменения)
- ↑↓ - существующие индикаторы трендов изменения

## Архитектурные изменения

### 1. Обновление ReplyKeyboard

**Файл:** `src/bot/ExchangeRatesBot.App/Phrases/BotPhrases.cs`

```csharp
public static string BtnStatistics { get; } = "Статистика";
```

Обновить `GetMainKeyboard()` в `CommandService.cs` до схемы 3x2:
```
[ Курс сегодня ] [ За 7 дней ] [ Статистика ]
[ Валюты       ] [ Подписка   ] [ Помощь    ]
```

### 2. Обновление интерфейса IMessageValute

**Файл:** `src/bot/ExchangeRatesBot.Domain/Interfaces/IMessageValute.cs`

```csharp
/// <summary>
/// Формирует компактное сообщение со статистикой за указанный период.
/// Использует разные форматы в зависимости от длины периода.
/// </summary>
Task<string> GetValuteStatisticsMessage(int days, string[] charCodesCollection, CancellationToken cancel);
```

### 3. Реализация GetValuteStatisticsMessage

**Файл:** `src/bot/ExchangeRatesBot.App/Services/MessageValuteService.cs`

Основной метод с выбором формата:
```csharp
public async Task<string> GetValuteStatisticsMessage(int days, string[] charCodesCollection, CancellationToken cancel)
{
    var sb = new StringBuilder();
    for (int i = 0; i < charCodesCollection.Length; i++)
    {
        var message = await GetValuteStatisticsMessageSingle(days, charCodesCollection[i], cancel);
        if (!string.IsNullOrEmpty(message))
        {
            sb.Append(message);
            if (i < charCodesCollection.Length - 1)
                sb.AppendLine();
        }
    }
    return sb.ToString();
}

private async Task<string> GetValuteStatisticsMessageSingle(int days, string charCode, CancellationToken cancel)
{
    var valutesRoot = await _processingService.RequestProcessing(days, charCode, cancel);
    if (valutesRoot == null || !valutesRoot.GetValuteModels.Any())
        return string.Empty;

    var valutes = valutesRoot.GetValuteModels
        .Select(item => new Valute
        {
            CharCode = item.CharCode,
            DateValute = item.DateValute,
            Name = item.Name,
            Value = item.Value
        })
        .GroupBy(e => e.DateValute.Date)
        .Select(g => g.First())
        .OrderByDescending(v => v.DateValute)
        .ToList();

    if (valutes.Count < 2)
        return string.Empty;

    var stats = CalculateStatistics(valutes.OrderBy(v => v.DateValute).ToList());

    // Адаптивный выбор формата
    if (days <= 7)
        return FormatShortPeriodStatistics(valutes, stats);
    else if (days <= 14)
        return FormatMediumPeriodStatistics(valutes, stats);
    else
        return FormatLongPeriodStatistics(valutes, stats);
}
```

### 4. Методы форматирования для разных периодов

#### FormatShortPeriodStatistics (1-7 дней)

```csharp
private string FormatShortPeriodStatistics(List<Valute> valutes, ValuteStatistics stats)
{
    var sb = new StringBuilder();
    sb.AppendLine($"{stats.CharCode}/RUB -- {stats.Name}");
    sb.AppendLine("━━━━━━━━━━━━━━━━━");

    foreach (var valute in valutes)
    {
        sb.AppendLine(FormatValuteLine(valute));
    }

    if (stats.DaysCount >= 2)
    {
        sb.AppendLine();
        sb.AppendLine($" Макс: {stats.MaxValue:0.00} ({stats.MaxDate:dd MMM})");
        sb.AppendLine($" Мин:  {stats.MinValue:0.00} ({stats.MinDate:dd MMM})");
        sb.AppendLine($" Изм:  {FormatChange(stats.AbsoluteChange, stats.PercentChange)} за {stats.DaysCount} дн.");
    }

    return sb.ToString();
}
```

#### FormatMediumPeriodStatistics (8-14 дней)

```csharp
private string FormatMediumPeriodStatistics(List<Valute> valutes, ValuteStatistics stats)
{
    var sb = new StringBuilder();
    sb.AppendLine($"📊 {stats.CharCode}/RUB -- {stats.Name} ({stats.DaysCount} дней)");
    sb.AppendLine();

    sb.AppendLine("📊 Последние дни:");
    foreach (var valute in valutes.Take(3))
    {
        sb.AppendLine($" {FormatValuteLine(valute)}");
    }

    sb.AppendLine();
    sb.AppendLine("📈 Статистика за период:");
    sb.AppendLine($" ↑ Макс: {stats.MaxValue:0.00} ({stats.MaxDate:dd MMM})");
    sb.AppendLine($" ↓ Мин:  {stats.MinValue:0.00} ({stats.MinDate:dd MMM})");
    sb.AppendLine($" ↔ Изм:  {FormatChange(stats.AbsoluteChange, stats.PercentChange)} за {stats.DaysCount} дн.");

    return sb.ToString();
}
```

#### FormatLongPeriodStatistics (15-30 дней)

```csharp
private string FormatLongPeriodStatistics(List<Valute> valutes, ValuteStatistics stats)
{
    var sb = new StringBuilder();
    sb.AppendLine($"📊 {stats.CharCode}/RUB -- {stats.Name} ({stats.DaysCount} дней)");
    sb.AppendLine();
    sb.AppendLine($"💰 Текущий: {stats.CurrentValue:0.00} руб.");
    sb.AppendLine();
    sb.AppendLine("📈 Динамика:");
    sb.AppendLine($" ↑ Макс: {stats.MaxValue:0.00} руб. ({stats.MaxDate:dd MMM})");
    sb.AppendLine($" ↓ Мин:  {stats.MinValue:0.00} руб. ({stats.MinDate:dd MMM})");
    sb.AppendLine($" ↔ Изм:  {FormatChange(stats.AbsoluteChange, stats.PercentChange)}");
    sb.AppendLine();
    sb.AppendLine("📉 Тренд за период:");
    sb.AppendLine($" Начало периода: {valutes.Last().Value:0.00} руб.");
    sb.AppendLine($" Конец периода:  {valutes.First().Value:0.00} руб.");

    var avgValue = valutes.Average(v => v.Value);
    sb.AppendLine($" Средний курс:   {avgValue:0.00} руб.");

    return sb.ToString();
}
```

#### FormatChange (вспомогательный)

```csharp
private string FormatChange(double absoluteChange, double percentChange)
{
    if (Math.Abs(absoluteChange) < 0.01)
        return $"{absoluteChange:0.00} ({percentChange:0.00}%)";

    var sign = absoluteChange >= 0 ? "+" : "";
    return $"{sign}{absoluteChange:0.00} ({sign}{percentChange:0.00}%)";
}
```

### 5. Обработка команд в CommandService

**Файл:** `src/bot/ExchangeRatesBot.App/Services/CommandService.cs`

#### Добавить обработчик в MessageCommand:

```csharp
case "/statistics":
case var txt when txt == BotPhrases.BtnStatistics:
    await _updateService.EchoTextMessageAsync(
        update,
        "📊 Выберите период для статистики:",
        new InlineKeyboardMarkup(PeriodSelectionKeyboard()));
    break;
```

#### Добавить метод генерации inline клавиатуры:

```csharp
private static List<List<InlineKeyboardButton>> PeriodSelectionKeyboard()
{
    return new List<List<InlineKeyboardButton>>
    {
        new List<InlineKeyboardButton>
        {
            InlineKeyboardButton.WithCallbackData("3 дня", "period_3"),
            InlineKeyboardButton.WithCallbackData("7 дней", "period_7"),
            InlineKeyboardButton.WithCallbackData("14 дней", "period_14")
        },
        new List<InlineKeyboardButton>
        {
            InlineKeyboardButton.WithCallbackData("21 день", "period_21"),
            InlineKeyboardButton.WithCallbackData("30 дней", "period_30")
        }
    };
}
```

#### Добавить обработчик callback в CallbackMessageCommand:

```csharp
if (callbackData.StartsWith("period_"))
{
    var days = int.Parse(callbackData.Substring(7));
    var currencies = _userControl.GetUserCurrencies(_userControl.CurrentUser.ChatId);

    await _updateService.EchoTextMessageAsync(
        update,
        await _valuteService.GetValuteStatisticsMessage(days, currencies, CancellationToken.None),
        default);

    await _botService.Client.AnswerCallbackQueryAsync(update.CallbackQuery.Id);
    return;
}
```

### 6. Обновление HelpMessage

**Файл:** `src/bot/ExchangeRatesBot.App/Phrases/BotPhrases.cs`

```csharp
public static string HelpMessage { get; } =
    "*Доступные команды:*\n\r" +
    "Курс сегодня -- курсы валют ЦБ РФ на сегодня\n\r" +
    "За 7 дней -- изменения курсов за последние 7 дней\n\r" +
    "Статистика -- детальная статистика за период (3-30 дней)\n\r" +
    "Валюты -- выбор валют для отслеживания\n\r" +
    "Подписка -- подписаться/отписаться от рассылки курсов\n\r" +
    "Помощь -- это сообщение\n\r\n\r" +
    "_Также доступны команды:_ /valuteoneday, /valutesevendays, /statistics, /currencies, /subscribe, /help";
```

## Список файлов для изменения

1. **src/bot/ExchangeRatesBot.App/Phrases/BotPhrases.cs**
   - Добавить константу `BtnStatistics`
   - Обновить `HelpMessage`

2. **src/bot/ExchangeRatesBot.Domain/Interfaces/IMessageValute.cs**
   - Добавить метод `GetValuteStatisticsMessage`

3. **src/bot/ExchangeRatesBot.App/Services/MessageValuteService.cs**
   - Реализовать `GetValuteStatisticsMessage`
   - Добавить `GetValuteStatisticsMessageSingle`
   - Добавить `FormatShortPeriodStatistics`
   - Добавить `FormatMediumPeriodStatistics`
   - Добавить `FormatLongPeriodStatistics`
   - Добавить `FormatChange`

4. **src/bot/ExchangeRatesBot.App/Services/CommandService.cs**
   - Обновить `GetMainKeyboard()` (схема 3x2)
   - Добавить обработчик `/statistics` и `BtnStatistics` в `MessageCommand`
   - Добавить метод `PeriodSelectionKeyboard()`
   - Добавить обработчик `period_*` в `CallbackMessageCommand`

## Порядок реализации

### Этап 1: Подготовка фраз (5 мин)
1. Добавить `BtnStatistics` в BotPhrases.cs
2. Обновить `HelpMessage`

### Этап 2: Обновление интерфейса (5 мин)
3. Добавить метод `GetValuteStatisticsMessage` в IMessageValute.cs

### Этап 3: Реализация сервиса сообщений (30 мин)
4. Реализовать `GetValuteStatisticsMessage` в MessageValuteService.cs
5. Реализовать `GetValuteStatisticsMessageSingle`
6. Реализовать `FormatShortPeriodStatistics`
7. Реализовать `FormatMediumPeriodStatistics`
8. Реализовать `FormatLongPeriodStatistics`
9. Реализовать `FormatChange`

### Этап 4: Обработка команд (20 мин)
10. Обновить `GetMainKeyboard()` в CommandService.cs
11. Добавить обработчик `/statistics` и `BtnStatistics`
12. Реализовать `PeriodSelectionKeyboard()`
13. Добавить обработчик callback `period_*`

### Этап 5: Тестирование (15 мин)
14. Сборка: `dotnet build src/ExchangeRates.Api.sln`
15. Локальное тестирование всех периодов (3, 7, 14, 21, 30 дней)
16. Проверка форматов сообщений

### Этап 6: Docker и прод (10 мин)
17. Пересборка контейнеров: `docker-compose up -d --build`
18. Интеграционное тестирование в @FragebogerDevBot

## Критерии приемки

### Функциональные
1. ✅ Кнопка "Статистика" в ReplyKeyboard при `/start`
2. ✅ Команда `/statistics` работает
3. ✅ Inline клавиатура с вариантами периодов показывается
4. ✅ Выбор 3, 7, 14, 21, 30 дней работает
5. ✅ Форматы адаптируются по длине периода
6. ✅ Эмодзи используются корректно

### Технические
7. ✅ Сборка без ошибок
8. ✅ Без миграций БД
9. ✅ Существующие команды работают без регрессии
10. ✅ Docker контейнеры запускаются

### UX
11. ✅ Интуитивно понятный интерфейс
12. ✅ Сообщения читаемы и красиво форматированы
13. ✅ Помощь содержит описание новой команды

## Обратная совместимость

- Все существующие команды работают без изменений
- Клавиатура расширяется (не удаляет старые кнопки)
- Форматы для 1-7 дней сохранены
- Без миграций БД
